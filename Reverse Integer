class Solution:
    def reverse(self,um ):
        num = int(abs(um))
        num = str(num)
        num = num[::-1]
        num = int(num)
        if num >= 2**31-1 or num <= -2**31:
            return 0
        elif um<0:
            return num*-1
        else:
            return num
